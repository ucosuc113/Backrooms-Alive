/**
 * Paquete reservado para la capa de red del mod (canales, paquetes
 * cliente-servidor).
 * <p>
 * Sin implementacion todavia. No se crea ningun canal aqui para evitar
 * registrar paquetes que dependan de clases que todavia no existen; se
 * completara cuando un modulo futuro necesite sincronizar datos.
 */
package com.glados.backrooms.network;
